# aic-web-page React migration

This workspace now contains a Vite + React + TypeScript scaffold. Run the dev server with:

```bash
npm install
npm run dev
```

The original static `index.html` has been backed up to `original-index.html`.
# aic-web-page
La pagina web del negocio de mi mama
